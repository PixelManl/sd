# UV Boot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the repo start from a stable demo script and manage dependencies with `uv`.

**Architecture:** Keep the project script-first. Add a thin standard-library configuration layer, then define dependencies in `pyproject.toml`, and verify with a dry-run command before attempting full model execution.

**Tech Stack:** Python 3.13, uv, unittest, diffusers, torch, Pillow, OpenCV

---

### Task 1: Lock startup configuration behavior

**Files:**
- Create: `tests/test_project_boot.py`
- Create: `project_boot.py`

- [ ] **Step 1: Write the failing test**
- [ ] **Step 2: Run the tests and verify red**
- [ ] **Step 3: Implement minimal config parsing**
- [ ] **Step 4: Re-run the tests and verify green**

### Task 2: Make the demo entrypoint bootable without model imports

**Files:**
- Modify: `generate_defect.py`
- Test: `tests/test_project_boot.py`

- [ ] **Step 1: Write the failing dry-run smoke test**
- [ ] **Step 2: Run the tests and verify red**
- [ ] **Step 3: Refactor imports and add `--dry-run` support**
- [ ] **Step 4: Re-run the tests and verify green**

### Task 3: Add uv project metadata

**Files:**
- Create: `pyproject.toml`
- Create: `.python-version`

- [ ] **Step 1: Add runtime dependencies**
- [ ] **Step 2: Mark the repo as a non-packaged uv project**
- [ ] **Step 3: Add dev dependencies for future test runs**

### Task 4: Publish the new startup path

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add a quick start section**
- [ ] **Step 2: Document `uv` installation and sync**
- [ ] **Step 3: Document dry-run and full-run commands**

### Task 5: Verify end-to-end

**Files:**
- None

- [ ] **Step 1: Run unit tests**
- [ ] **Step 2: Install uv if missing**
- [ ] **Step 3: Create or sync the uv environment**
- [ ] **Step 4: Run `uv run python generate_defect.py --dry-run`**
